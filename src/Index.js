import React from 'react'

import { AppRoot } from '@components'
import { BottomTabNavigation } from '@navigation'

const App = () => {
  return (
    <AppRoot>
      <BottomTabNavigation />
    </AppRoot>
  );
}

export default App
